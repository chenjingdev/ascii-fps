{
  "name": "ascii-fps",
  "version": "0.2.0",
  "description": "A terminal ASCII FPS using terminal-kit for terminal control and custom game/render logic.",
  "type": "module",
  "files": [
    "ascii-fps",
    "src",
    "README.md"
  ],
  "bin": {
    "ascii-fps": "./ascii-fps"
  },
  "scripts": {
    "start": "node ./ascii-fps",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "terminal-kit": "^3.1.2"
  }
}
